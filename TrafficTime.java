//A simple enum that represents the traffic flow time.
//We use it to get traffic flow from TrafficData based on time.
public enum TrafficTime {
    MORNING,
    AFTERNOON,
    EVENING,
    NIGHT
}

